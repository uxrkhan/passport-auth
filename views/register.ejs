<div class="row mt-5 mb-5">
    <div class="col-md-6 m-auto">
        <div class="card card-body px-5">
            <h1 class="text-center my-3"><i class="fas fa-user-plus text-success"></i> Register</h1>

            <%- include('./partials/messages.ejs') %>

            <form action="/users/register" method="POST">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input 
                        type="name"
                        name="name"
                        placeholder="Enter name"
                        id="name"
                        class="form-control form-control-sm"
                        value="<%= typeof name != 'undefined' ? name : '' %>">
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input 
                        type="email"
                        name="email"
                        placeholder="Enter email"
                        id="email"
                        class="form-control form-control-sm"
                        value="<%= typeof email != 'undefined' ? email : '' %>"> 
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input 
                        type="password"
                        name="password"
                        placeholder="Enter password"
                        id="password"
                        class="form-control form-control-sm"
                        value="<%= typeof password != 'undefined' ? password : '' %>">
                </div>
                <div class="form-group">
                    <label for="password2">Confirm password</label>
                    <input 
                        type="password"
                        name="password2"
                        placeholder="Confirm password"
                        id="password2"
                        class="form-control form-control-sm"
                        value="<%= typeof password2 != 'undefined' ? password2 : '' %>">
                </div>
                <button class="btn btn-success btn-block mt-5" type="submit">Register</button>
            </form>
            <p class="text-center mt-3">Already have an account?  <a href="/users/login">Log In</a></p>
        </div>
    </div>
</div>